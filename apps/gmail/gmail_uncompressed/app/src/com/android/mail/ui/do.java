package com.android.mail.ui;

import android.os.*;

final class do implements Parcelable$ClassLoaderCreator<ToastBarOperation>
{
    private static ToastBarOperation b(final Parcel parcel, final ClassLoader classLoader) {
        return new ToastBarOperation(parcel, classLoader);
    }
}
