package com.google.android.gm.ads;

import com.android.mail.ui.*;
import android.os.*;

final class p implements Parcelable$ClassLoaderCreator<ToastBarOperation>
{
    private static ToastBarOperation b(final Parcel parcel, final ClassLoader classLoader) {
        return new AdToastBarOperation(parcel, classLoader, (byte)0);
    }
}
