package android.support.v4.content;

import android.content.*;

class h extends g
{
    @Override
    public final Intent makeMainActivity(final ComponentName componentName) {
        return Intent.makeMainActivity(componentName);
    }
}
